---
interface Contact {
  type: 'email' | 'marshmallow';
  href: string;
  icon: string;
  text: string;
}

const contacts: Contact[] = [
  {
    type: 'email',
    href: 'mailto:kaigai.career.log@gmail.com',
    icon: '📧',
    text: 'Email'
  },
  {
    type: 'marshmallow',
    href: 'https://marshmallow-qa.com/pnyvljc4559xpz9?t=A6BDdq&utm_medium=url_text&utm_source=promotion',
    icon: '🍬',
    text: 'マシュマロ'
  }
];
---

<section class="contact-section">
  <h2>お問い合わせ・質問</h2>
  <div class="contact-grid">
    {contacts.map((contact) => (
      <a 
        href={contact.href}
        target={contact.type === 'marshmallow' ? '_blank' : undefined}
        rel={contact.type === 'marshmallow' ? 'noopener noreferrer' : undefined}
        class={`contact-link ${contact.type}`}
      >
        <span class="contact-icon">{contact.icon}</span>
        <span class="contact-text">{contact.text}</span>
      </a>
    ))}
  </div>
</section>

<style>
  .contact-section {
    margin-top: 3rem;
    padding-top: 3rem;
    border-top: 1px solid rgba(59, 130, 246, 0.1);
  }

  .contact-section h2 {
    font-size: 1.3rem;
    font-weight: 500;
    text-align: center;
    margin-bottom: 1.5rem;
    color: var(--color-muted);
  }

  .contact-grid {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    max-width: 500px;
    margin: 0 auto;
  }

  .contact-link {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    padding: 1.5rem 2rem;
    background: var(--color-background);
    border: 2px solid rgba(59, 130, 246, 0.15);
    border-radius: 1rem;
    text-decoration: none;
    color: var(--color-text);
    transition: all 0.3s ease;
    flex: 1;
  }

  .contact-link:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 24px rgba(59, 130, 246, 0.2);
    border-color: rgba(59, 130, 246, 0.3);
  }

  .contact-icon {
    font-size: 2rem;
  }

  .contact-text {
    font-weight: 600;
    font-size: 1rem;
  }

  .contact-link.email:hover {
    background: rgba(59, 130, 246, 0.05);
  }

  .contact-link.marshmallow:hover {
    background: rgba(255, 182, 193, 0.1);
    border-color: rgba(255, 182, 193, 0.5);
  }

  /* Mobile responsive */
  @media (max-width: 768px) {
    .contact-grid {
      flex-direction: column;
      gap: 1rem;
    }

    .contact-link {
      padding: 1.25rem 1.5rem;
    }
  }

  /* Dark mode support */
  @media (prefers-color-scheme: dark) {
    .contact-link {
      background: rgba(15, 23, 42, 0.8);
      border-color: rgba(59, 130, 246, 0.2);
    }

    .contact-section {
      border-top-color: rgba(59, 130, 246, 0.2);
    }
  }
</style>

